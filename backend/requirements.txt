fastapi==0.100.0
uvicorn[standard]==0.22.0
psycopg2-binary==2.9.7
redis==4.5.5
python-dotenv==1.0.0
pytest==7.4.0
httpx==0.24.0

# ============================================================
# QuerySafe Backend — Python Dependencies
# ============================================================
# Generated for Person 1 (AI/LLM) module.
# Install with: pip install -r requirements.txt
# ============================================================

# ── Web Framework ────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
python-multipart==0.0.9

# ── LangChain / LangGraph ────────────────────────────────────
langchain==0.2.1
langchain-core==0.2.1
langchain-community==0.2.1
langgraph==0.1.5

# ── LLM Providers (both; active one is chosen via AI_PROVIDER) ──
langchain-openai==0.1.8
langchain-anthropic==0.1.15
openai==1.30.1
anthropic==0.28.0

# ── Vector Store — ChromaDB ──────────────────────────────────
chromadb==0.5.0

# ── Embeddings — Sentence Transformers ───────────────────────
sentence-transformers==3.0.0
torch==2.3.0              # CPU-only build; swap for torch+cu121 on GPU nodes
transformers==4.41.1
huggingface-hub==0.34.0

# ── Session Memory — Redis ───────────────────────────────────
redis==5.0.4

# ── HTTP Client (internal service calls) ─────────────────────
httpx==0.27.0

# ── Async support ────────────────────────────────────────────
anyio==4.4.0

# ── Data / Utilities ─────────────────────────────────────────
numpy==1.26.4
python-dotenv==1.0.1
